"use client";

export function Header() {
  // Header is now minimal - theme is in settings
  return (
    <header className="hidden lg:flex fixed top-0 right-0 h-14 z-30 items-center justify-end px-6 bg-transparent">
      {/* Minimal header - main controls moved to sidebar/settings */}
    </header>
  );
}
