# ═══════════════════════════════════════════════════════════════
# CORTEXIA FRONTEND — ENVIRONMENT VARIABLES
# ═══════════════════════════════════════════════════════════════
#
# Copy this file to .env.local and fill in the values:
#   cp .env.local.example .env.local
#
# ─── SUPABASE (REQUIRED) ───────────────────────────────────────
#
# 1. Go to https://supabase.com → sign in → open your project
# 2. In the sidebar, click ⚙️ "Project Settings"
# 3. Click "API" under the Configuration section
#
# Project URL  → under "Project URL" you'll see:
#   https://xxxxxxxxxxxx.supabase.co
#
# Anon Key     → under "Project API keys" copy the one labeled:
#   "anon public"  (starts with eyJh...)
#
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ─── BACKEND API URL (REQUIRED) ───────────────────────────────
#
# The URL of your CorteXia backend (Hono server).
# - Local development: http://localhost:3001
# - Production (Render): https://your-app-name.onrender.com
#
NEXT_PUBLIC_API_URL=http://localhost:3001

# ─── GOOGLE OAUTH (configured in Supabase dashboard) ──────────
#
# You do NOT need env vars for Google OAuth.
# Instead, configure it inside Supabase:
#
# 1. Supabase Dashboard → Authentication → Providers
# 2. Enable "Google"
# 3. Go to https://console.cloud.google.com
#    → APIs & Services → Credentials → Create OAuth 2.0 Client ID
# 4. Set Authorized redirect URI to:
#      https://your-project-id.supabase.co/auth/v1/callback
# 5. Copy the Client ID and Client Secret back into Supabase
#
# That's it — no env vars needed here, Supabase handles it.
